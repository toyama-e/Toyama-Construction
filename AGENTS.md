# 株式会社 外山建設 コーポレートサイト

## このファイルの役割

このリポジトリで Codex が作業するときのプロジェクト指示です。
古いドキュメントと現在の実装が食い違う場合は、動作しているソースコードと本ファイルを優先してください。
仕様が未確定の項目は推測で本番情報に置き換えず、Mockまたは確認中であることを維持してください。

## プロジェクトの目的

株式会社 外山建設（新潟県三条市）の公式ホームページです。

優先順位は次の通りです。

1. 作業員を1名採用する
2. 工事の相談・依頼を獲得する
3. 小さな会社ならではの人柄、親しみやすさ、安心感を伝える

採用を主目的としつつ、営業案内と問い合わせ導線も提供します。

## ターゲットとデザイン方針

- 主な採用ターゲットは20代〜40代くらいの男性
- 閲覧はスマートフォンを最優先とする
- 華やかにしすぎず、親しみと信頼感のあるデザインにする
- 社長と役員の2人を中心に運営する小さな会社として、入りやすい雰囲気を伝える
- 本番では社長と役員の顔写真を掲載し、人柄や職場の空気が伝わる構成にする
- 現在の人物写真はMockであり、本番用の写真ではない
- サイト全体は「温かいモノトーン＋オレンジ」で統一する
- 写真は白黒加工せず、自然なカラー写真を使用する
- 背景は温かいアイボリー、本文と主要CTAはチャコール、カードはグレージュを使用する
- オレンジは見出し、下線、番号などの小さなアクセントに限定する
- LINEのボタンはサービスを認識しやすいよう公式の緑を維持する

### カラーパレット

| 用途           | 色                 | カラーコード |
| -------------- | ------------------ | ------------ |
| 背景           | ウォームアイボリー | `#F7F4EE`    |
| カード・区切り | ライトグレージュ   | `#F0ECE6`    |
| 本文・主要CTA  | チャコール         | `#202020`    |
| 補助テキスト   | ウォームグレー     | `#5F5A54`    |
| アクセント     | バーントオレンジ   | `#B84F2D`    |

色を追加する場合も、華やかにしすぎず、写真と余白が主役になるシンプルなデザインを維持してください。

## 現在のページ構成

- `/`：営業・会社案内ページ
  - Header
  - Hero
  - Services
  - WhyUs
  - License
  - About
  - Contact
  - Footer
- `/recruit`：採用専用ページ
  - Header
  - Hero
  - JobSpec
  - Promise
  - DailySchedule
  - Message
  - Faq
  - Apply
  - Footer

施工実績セクションは将来追加予定で、現時点では実装しません。

## 技術構成

- Next.js App Router
- TypeScript
- Tailwind CSS
- 静的エクスポート（`output: "export"`）
- Vitest / Testing Library
- ESLint / Prettier
- Cloudflare Pagesでの公開を予定（未運用）

## 確定している会社・問い合わせ仕様

- 一般公開する固定電話番号はない
- LINE通話を電話窓口として扱う
- `src/constants/company.ts` にあるLINE通話URLは確定済み
- `src/constants/company.ts` にあるLINE友だち追加URLは確定済み
- 問い合わせ方法はLINE通話、LINEチャット、メール
- GoogleマップはAPIキー不要のiframe埋め込みを使用する
- FAX番号を電話番号として表示・構造化データへ登録しない
- 建設業許可番号は「新潟県知事許可（般-4）第46299号」
- 加入保険は「建設工事保険」「労働者災害補償保険」
- 現在掲載中の保有資格はMockのまま使用する
- 現在掲載中の採用条件・募集要件はMockのまま使用する

## 確認中・Mockの情報

次の情報は確認が完了するまで確定値として扱わないでください。

- 本番ドメイン：未取得
- 保有資格：Mock
- 採用条件（給与、勤務時間、休日、補償、資格支援など）：Mock
- 施工対応地域の具体的な市町村名：確認中
- 人物写真：現在はMock
- Cloudflare Pages：公開予定だが未運用

これらを変更する場合は、ユーザーから明示された確定情報だけを使用してください。

## スマートフォン優先ルール

- 320px幅程度からレイアウトが崩れないようにする
- 主要なリンク・ボタンのタップ領域は最低44px × 44pxを目安にする
- 採用CTAを営業CTAより優先する
- LINEへのリンクは、スマートフォンではQRコードよりタップ導線を優先する
- 長い表は小さい画面で横にはみ出さないよう、縦積みも検討する
- 固定ヘッダーによって見出しやアンカー先が隠れないようにする
- 文字サイズは本文16px前後を基本とし、小さすぎる補足文を多用しない
- hoverだけに依存せず、タッチ・キーボードでも操作できるようにする
- `prefers-reduced-motion` を尊重する

## コーディング規約

- 原則として1ファイル1コンポーネント
- コンポーネント名とファイル名はPascalCase
- 既存のApp Router構成と静的エクスポートを維持する
- スタイリングはTailwind CSSを基本とする
- 会社情報、連絡先、採用条件など再利用する値は定数へ集約する
- 会社情報は原則 `src/constants/company.ts` を唯一の情報源にする
- ContactとApplyなど、同じUI・処理の重複は共通化を検討する
- 装飾目的の画像は空のalt、意味のある画像には内容が分かるaltを設定する
- 外部リンクを新しいタブで開く場合は `rel="noopener noreferrer"` を付ける
- 固定電話がないため、架空の電話番号や `tel:` リンクを追加しない
- Mock情報を事実のように補完しない

## 画像の扱い

- 保存先は `public/images/`
- 新しいファイル名は小文字・ハイフン区切りを基本とする
- スマートフォン配信を考慮し、写真は原則WebPまたはAVIFへ圧縮する
- 1枚200KB以下を目安とし、画質とのバランスを確認する
- LCP対象画像は特に小さくし、適切なサイズ指定を行う
- 静的エクスポートでは `next/image` のサーバー側最適化が使えないため、素材自体を事前に最適化する
- 写真差し替え時は、掲載許可と人物が本番用かを確認する
- 参照する画像ファイルが実在することを確認する

## SEO・構造化データ

- 本番ドメインが確定するまで、仮URLを本番値として固定しない
- 公開前にmetadata、Open Graph、sitemap、robots、JSON-LDのURLを本番ドメインへ合わせる
- 存在しないOGP画像を参照しない
- LocalBusinessの `telephone` にFAX番号を設定しない
- FAXを構造化データへ載せる場合は `faxNumber` を使用する
- トップページと採用ページで適切なtitleとdescriptionを設定する

## アクセシビリティ

- セマンティックなHTMLと正しい見出し順を維持する
- リンクとボタンに見えるフォーカス表示を付ける
- 色だけで状態や意味を伝えない
- 本文と背景のコントラストを確保する
- ナビゲーションには必要に応じて分かりやすい `aria-label` を付ける
- 自動アニメーションは動きを減らす設定に対応する

## プライバシー

LINEやメールで氏名、連絡先、応募内容、工事相談内容を受け取るため、公開前にプライバシーポリシーの掲載を検討してください。
少なくとも利用目的、第三者提供、安全管理、開示・訂正・削除の窓口を明記します。

## 作業時の確認

変更後は内容に応じて次を実行してください。

```bash
npm test -- --run
npm run lint
npm run build
```

- テストやlintの既存エラーを無関係に隠さない
- 主要リンク、LINE通話、友だち追加、メールのhrefを確認する
- 画像やOGPなどの参照先が存在することを確認する
- UI変更時はスマートフォン幅を最優先で確認し、PC表示も壊していないことを確認する
- 公開前にはiPhone Safari、Android Chrome、PCで実機または同等の表示確認を行う

## Git運用

- 作業は `develop` ブランチで行う
- 変更はGitHubの `develop` ブランチへpushする
- Pull Requestは `develop` ブランチから `main` ブランチに向けて作成する
- 新しい作業ブランチは、ユーザーから明示的な指示がない限り作成しない
- コミット、push、Pull Request作成は、ユーザーの依頼がある場合だけ行う
- ユーザーの既存変更を上書き・破棄しない
- コミット前に差分とテスト結果を確認する
